with "../../config.gpr";
with "../../boards/stm32f429_discovery.gpr";

project Conway extends "../conway/conway_generic" is

   for Runtime ("Ada") use Config.RTS & "-stm32f429disco";
   for Object_Dir use "obj/conway";

end Conway;
